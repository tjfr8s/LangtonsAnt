#ifndef RUNLANGTON_HPP
#define RUNLANGTON_HPP
#include <iostream>
#include <unistd.h>
#include "Board.hpp"

int  getMenu();
void runMenu();
void runLangton(int steps, int numRows, int numCols);

#endif
